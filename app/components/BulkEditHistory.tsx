import React from "react";

interface BulkEditHistoryProps {
  isVisible?: boolean;  // Made optional and will be ignored
}

// Component completely removed - no longer showing activity info
export function BulkEditHistory(_props: BulkEditHistoryProps): JSX.Element {
  return <></>;
}
